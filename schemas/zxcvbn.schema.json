{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "zxcvbn.schema.json",
  "title": "zxcvbn — Password Strength",
  "description": "Check password strength using the zxcvbn algorithm. Passwords with entropy >= 75.0 are considered strong.",
  "definitions": {
    "get": {
      "title": "zxcvbn.get",
      "description": "Evaluate the strength of a password and return its entropy score.",
      "request_params": {
        "type": "object",
        "properties": {
          "password": {
            "type": "string",
            "description": "Password to evaluate"
          }
        },
        "required": ["password"]
      },
      "response_result": {
        "type": "object",
        "properties": {
          "entropy": {
            "type": "number",
            "description": "Entropy score. Values >= 75.0 indicate a strong password.",
            "minimum": 0
          }
        },
        "required": ["entropy"]
      },
      "example_request": {
        "id": "req-1",
        "jsonrpc": "2.0",
        "method": "call",
        "params": ["<sid>", "zxcvbn", "get", {"password": "1H_$X%rIjg(EIu+ecw9VCZ+T"}]
      },
      "example_response": {
        "id": "req-1",
        "jsonrpc": "2.0",
        "result": [0, {"entropy": 132.7}]
      }
    }
  }
}
